_schema-version: "3.2"
ID: progferieMAN
description: A Fiori application.
version: 0.0.1
modules:
- name: progferieMAN-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: progferieMAN-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - progferieMAN.zip
      name: progferieMAN
      target-path: resources/
- name: progferieMAN
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: progferieMAN-approuter
  type: approuter.nodejs
  path: progferieMAN-approuter
  requires:
  - name: progferieMAN_html_repo_runtime
  - name: uaa_progferieMAN
  parameters:
    disk-quota: 256M
    memory: 256M
- name: progferieMAN_ui_deployer
  type: com.sap.application.content
  path: .
resources:
- name: progferieMAN-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: progferieMAN-html5-srv
    service-plan: app-host
- name: progferieMAN-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: progferieMAN-destination-service
    service-plan: lite
- name: progferieMAN_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_progferieMAN
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: progferieMAN-xsuaa-ervsice
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
